const A = 12
const B = 22
const C = 21

soma = A + B
if (soma < C) {
    console.log(`A soma de ${A} e ${B} é menor que ${C}`)
} else {
    console.log(`A soma de ${A} e ${B} é maior que ${C}`)
}